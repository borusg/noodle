#!/bin/bash

[[ -z $NOODLE_SERVER ]] && NOODLE_SERVER=localhost:9292
URL="http://${NOODLE_SERVER}/nodes"

if [[ $(basename $0) == 'noodlin' ]]
then
    URL="${URL}/noodlin/"
else
    URL="${URL}/_/"
fi

curl -s -G $URL --data-urlencode "$*"

